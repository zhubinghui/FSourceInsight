# FSourceInsight 新闻采集领域

本文统一新闻采集、规则验证与人工发布的用语，不表示所有能力均已实现。

## 领域语言

**新闻来源（News source）**：被跟踪的新闻发布渠道，是采集规则和文章归属的业务范围。

**采集档案（Crawl source profile）**：一个来源中具有共同目标类型、语言或页面族的采集范围，以及这一范围所采用的采集策略。
_Avoid_：用“质量档案”称呼采集档案。

**采集策略（Source policy）**：管理员为采集范围明确授予的访问边界及可信质量标准，不由抽取规则、网页或模型自行扩张。
_Avoid_：把一次预览时的临时许可当作持续有效的来源策略。

**策略版本（Policy version）**：一次不可变的策略授予或撤销决定及其适用的来源条件；新决定不改写过去的决定。

**撤销策略（Policy revocation）**：明确撤回采集策略的决定，不表示删除历史，也不自动恢复较早的许可。

**抽取规则（Recipe）**：描述从列表、Feed和详情中读取文章字段的声明式规则，不代表访问许可或发布批准。
_Avoid_：把模型生成的可执行爬虫代码称作recipe。

**规则版本（Schema version）**：抽取规则在验证、审批、使用和追溯中的不可变版本；审批状态变化不改变其规则内容。

**候选版本（Candidate）**：等待验证或人工决定是否采用的规则版本。合格候选不等于当前生效版本。

**当前版本（Active version）**：某采集档案当前批准使用的规则版本。
_Avoid_：把“最新创建”或“最近验证通过”的版本直接称为active。

**上一版本（Previous version）**：被当前版本替换的规则版本，是人工回滚的候选目标，不代表在任何新策略下都仍然可用。

**质量标准（Quality profile）**：判定抽取结果是否可用及正文级别的可信标准，不由recipe或网页自行声明。
_Avoid_：用“采集档案”指代正文长度、段落数等质量要求。

**内容级别（Content level）**：文章证据支持的内容范围，区分元数据、摘录与通过正文门槛的内容；未知不能冒充全文。

**网页快照（Page snapshot）**：某次采集所保留的页面内容及其上下文证据，用于复验和追溯，不代表来源真实性认证或入库授权。

**采样条目（Capture manifest）**：一次已保存Admin预览中，页面指纹与固定输入版本的不可变记录；不保存原文，不随有限预览报告裁剪而删除。
_Avoid_：把指纹台账称为原文快照、完整HTTP请求账本、独立验证或审批证明。

**采样历史覆盖（Capture history coverage）**：该Admin预览链从开始追踪至今的条目覆盖情况。旧历史未知不能被新样本自动补全；结构连续不等于所有文档已经逐条校验，也不覆盖其他采集/CLI/系统外看到的样本。

**学习会话（Repair session）**：管理员针对既定采样证据委托的一次有界规则学习；消息重投属于同一委托，不重新获得轮次或预算。取消不撤销已经发生的费用。

**学习冷却（Learning cooldown）**：学习执行失败或取消后，该来源暂不能发起新学习委托的保护期；换样本不视为另一个来源。

**受控重试（Controlled retry）**：管理员对原学习委托作出的明确、可追溯的再次执行请求；不补充轮次、预算或期限，也不把不确定费用视为尚未付费。
_Avoid_：把消息重投、重新领额度或不确定付费调用接管统称为重试。

**学习轮次（Repair attempt）**：会话内一次提案及训练检验，可能包含有限供应商备用尝试；供应商备用尝试同样占用会话额度。

**模型样本暴露（Model exposure）**：样本可能已提供给模型的保守记录；即使响应丢失，也不能把该样本重新认定为未见过的独立留出。
_Avoid_：把暴露指纹当作完整原文、确认送达或全系统历史覆盖证明。

**受控学习历史（Controlled learning history）**：本应用受控学习委托及可能送出样本的持久序列；检查覆盖、归属及指纹完整性，不因取消或原文清理而重置。
_Avoid_：把台账tracked称为所有模型的完整输入史、语义去重或独立验证通过。

**训练回放（Training replay）**：在已提供的证据上检查提案的抽取结果，不是独立留出验证，也不授予发布资格。

**冻结候选（Frozen candidate）**：规则内容和可采用样本的选择边界已固定、等待验证的候选；验证反馈不能反过来改写这份候选。

**采样清单（Sampling inventory）**：由学习前参考规则确定的待覆盖文章集合，是覆盖检查的参照，不是正文事实真值。
_Avoid_：把模型选出的好案例当作独立采样清单。

**独立留出（Independent holdout）**：在明确追踪范围内未用于学习或较早验证、由执行方为冻结候选选定的检验样本。
_Avoid_：把换URL的已见正文、训练回放或缺历史的样本称为独立留出。

**验证报告（Validation report）**：冻结候选在固定留出证据及质量标准下的检查结论；历史通过不表示证据和授权仍有效，也不是发布批准。

**企业目录来源（Startup source）**：提供企业名录/投资组合/研究机构条目的独立配置范围，不等同于新闻来源或新闻采集策略。

**初始分析意图（Initial analysis job）**：与一次真正新增的公司同事务保存、绑定目录来源和冻结输入的初次分析委托；消息丢失不会使它消失，消息重投不产生新付款资格。
_Avoid_：把历史公司自动补分析、手动refresh或学习会话混称为同一job。

**重派（Redispatch）**：再次传递同一尚未认领意图的身份，不是重新授权已运行的模型调用，也不重置输入/期限或清除未知费用。

**学习派发键（Learning delivery key）**：绑定会话输入指纹、已消耗轮次和人工重试计数的消息指纹；区分旧消息与当前queued阶段，不是权限凭证或新付费租约。
_Avoid_：把派发键、派发资格时间或消息发送成功称为供应商准入、收到确认或再次付款许可。

**逻辑采集（Crawl run）**：针对一个采集范围的一次工作委托；重投递和重试仍可属于同一次委托，而非新的业务采集。
