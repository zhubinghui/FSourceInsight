# M3.4c release: refresh jobs, holdout v2, learning byte bound, small fixes (2026-09-24 17:15 UTC)

Owner-authorized ("合入部署"). Application `a93beb8` → `7ec578c`; schema `c7f21a9d680e` → `d3e7a1c95b28`.
Plan and run-1 correction: `docs/superpowers/specs/2026-09-24-m34c-release.md`. This session could not write to
production, so the owner ran the reviewed script with `!`; all other checks were read-only.

## Contents

- M3.4c company refresh jobs: manual and article-triggered refreshes are durable, coalesced and fenced by
  company generation; no inline or automatic-retry payments.
- Lost-write fixes: revision history and merge aliases now persist.
- Date filters include the whole day; the company weekly sentiment chart uses Monday weeks in date order.
- Learning code, still disabled: holdout validation v2 and the learning input byte bound.
- Migration `d3e7a1c95b28`: one table and a nullable reservation column, expand-only.

## Evidence before release

CI 36023303297 on the same application tree: 1141 offline tests passed, 23 real MySQL tests passed, and the real
Admin/Redis/prefork recycling gate passed. Local full suite: 1141 passed, 23 skipped.

## Run 1 (16:59Z): aborted by a script bug, restored automatically

Images were built with runtime and dependencies identical to production. The backup
`m34c-20260924165933` was verified and the migration applied. The new web became healthy, but the check for task
registration could never pass. `celery inspect` streams replies per node, so `grep -q` in a `pipefail` pipe broke
it. The script restored all four apps from the ner pin at 17:02:43Z. A no-consume diagnostic container showed that
the candidate registered the task in 8 s. The script was corrected before the rerun.

## Run 2 (17:13Z): released

| Step | Result |
|---|---|
| Guards | Checkout fast-forwarded to `7ec578c`, no infra paths in the delta |
| Images | `candidate-m34c-20260924171323`; `python -VV` and `pip freeze` identical to the running containers for all four |
| Pin layer | Copy of the ner layer with only images changed; commands byte-identical |
| Quiesce | Beat stopped; both worker nodes replied with 0 active or reserved tasks; all queues empty |
| Backup | `~/fsourceinsight-backups/m34c-20260924171323/database.sql.gz`, 20507470 bytes, 0600, gzip and trailer verified, SHA-256 `d9a38071…3a71e` |
| Migration | No-op after run 1; current `d3e7a1c95b28 (head)`, `company_refresh_job` empty, new column present |
| Start | Web healthy, llm node registered `app.llm.refresh_tasks.refresh`, beat started last at 17:15:04Z |
| Ledger | Identical before and after: 2961 settled reservations, 624 usage rows today |

Run 2's before-snapshot equals run 1's, so nothing was paid while run 1's new apps were briefly up.

## Post-deploy (read-only)

All four apps on the new images with 0 restarts and no OOM. No Traceback, ERROR or CRITICAL lines. Public health,
company list, `/api/v1/news` and a one-day date filter all return 200. The new 60-second recovery task `app.llm.refresh_tasks.recover` ran on the llm worker and succeeded at 17:16:11Z. A company detail page returned 200, which exercises the new weekly-trend `DATE()` query on production MySQL for the first time.

## Rollback

Image rollback with the ner pin, as in the plan. The additive schema stays; the old code ignores it. Refresh jobs
created meanwhile stay inert, and old workers would pay inline after articles again.

## Not done

- No real model call, crawl or email was triggered for acceptance. The first article-triggered or manual refresh
  in production will be the first live exercise of the new job path.
- Learning remains disabled; enabling it needs a reviewed `crawl_schema` config, the flag and the dedicated worker.
- MySQL and broker gates inside the candidate images were not run; CI ran them on the same tree.
