# M0.5 提交与生产发布记录

## 结果

**已提交、推送并部署成功。**

- 用户明确授权“先完成一下提交部署”；本轮只发布已验收M0.5，不开展M1/Agent。
- 应用代码：`6451b3641cc6c8934d381aefd65f7efdb8d40d6f`（父提交d91863f），30文件变更。
- CI：[34036731442](https://github.com/zhubinghui/FSourceInsight/actions/runs/34036731442)，`test` / `mysql-integration` 两个job均success。
- 生产迁移：`c821b4f7d901 → d472ac9e6102`，model comparison **0 differences**。
- 应用切换：**2026-09-06 13:53:46Z → 13:53:51Z**，约5秒恢复Web健康；`LIVE_WORKERS_READY`确认两个worker就绪。
- 只更新web/worker/worker_fast/beat，未触发应用回滚。MySQL、Redis和同机其他原有Docker容器ID/StartedAt/RestartCount不变，Caddy保持active，无ai-router操作。
- 发布记录的后续文档提交仅同步checkout，不为文档重新构建镜像。

## 发布内容

独立LLM用量账本、结果收集后单事务应用、公司/分类幂等、严格JSON/结束原因校验、完整版本化缓存、有界fallback、Admin显式主备/优先级、共享CLI管线。实现详见 [M0.5记录](2026-09-06-m05-implementation.md)。

迁移只增加role/priority，不重跑seed。4个现有配置的model/tasks/active/default/温度/token/成本字段指纹在发布前后相同：

```text
e33b89e899f99e1bea1cc8255cfe566dfd9b6a9f0d404fa1ae8eb78a219733a5
```

全部按兼容默认role=primary/priority=100扩展，**没有静默更换生产模型**。新缓存不复用旧键，因此可能有冷启动调用；没有批量force重算文章。

## 发布前与候选验证

- 本地131 passed/10远程专用skipped，静态错误检查和diff检查通过；没有把skip算通过。
- 上一轮源码快照的独立MySQL两轮10/10是前置证据，见 [隔离验收](2026-09-06-m05-mysql-validation.md)。
- **真正新构建的候选镜像**：web和worker各自运行全部10项MySQL实测，通过，分别15.977/15.880秒；只挂载测试文件，不覆盖镜像业务源码。
- 四个应用候选均离线通过40模板编译、匿名有效CSRF拒绝、合成管理员路由页面、镜像无.env/.git/SQL dump检查。
- 候选依赖：Python3.12.14、Flask3.1.3、SQLAlchemy2.0.52、Alembic1.19.2、PyMySQL1.2.0、LiteLLM1.100.0。
- 专用MySQL8.0.46/internal网络/临时卷，无宿主端口；768MiB/1CPU。runner512MiB/1CPU、只读rootfs、cap-drop ALL、无生产凭据，LLM全部mock。临时资源及凭据在切换前已精确清理。

## 备份与镜像

备份目录：`/home/ubuntu/fsourceinsight-backups/m05-20260906134051`。

- `database.sql.gz`：**18,613,221 bytes，mode600**；single-transaction/quick导出，gzip完整性通过。
- SHA256：`b717eaf749f4e1485c496974db34b1608a2bb647b0c589309e3228c09597f7fa`。
- 备份未离开服务器、未读取内容、未进行全量恢复演练；不将压缩完整性检查称作恢复验证。
- 保留old-ref/release-ref、四个旧应用镜像tag、`rollback.compose.yml`、构建/迁移/候选/激活/worker/公网smoke日志、前后容器身份和发布脚本。`release-evidence.sha256`记录关键日志哈希。

| 服务 | 当前镜像SHA256 |
|---|---|
| web | `093e447ac122903302115f91aea49d42edbf0d64473ceb183cafae8b57c7efe0` |
| worker | `8369b86ed05ab04d09a23af2452a39ce95843059cc46b7732371be2820bd73d1` |
| worker_fast | `bd7714ae936a7882619c5b39d763e692c5a7e675bc747d29f4d12b88f22b206a` |
| beat | `11636afceede785e51b3c1eef8b9057d607f2c1caa2d8da3b08cbfcf21d9c5fa` |

旧镜像tag：`fsourceinsight-<service>:rollback-m05-20260906134051`；旧代码ref d91863f，旧运行应用来自858a14b。

## 切换与上线检查

1. CI、备份、候选通过后暂停beat，再次检查两worker active/reserved/scheduled均0。
2. `stop -t -1`等待warm shutdown，两个worker正常exit0，不强杀繁忙任务。
3. 保存到服务器的文件脚本执行Compose run（显式interactive=false/stdin），DDL metadata lock等待上限10秒。迁移后先检查head/model diff/配置指纹，再切镜像。
4. 新Web健康约5秒恢复，两个worker注册任务检查通过；运行的四个镜像ID逐一等于候选ID。
5. 公网首页、`/health`、`/auth/login`、www健康检查200；匿名有效CSRF settings POST和manage/Admin规范路径均302到登录。没有模拟生产管理员登录或改其设置。
6. 数据库/Redis健康、路由配置保留、其他容器身份未变；清理临时辅助文件，必要脚本与证据已归档到备份目录。

## 两次验证偏差（均保留记录）

- **候选收集权限**：准备脚本umask077使git更新源码为600，Docker COPY保留；额外使用UID1000的runner无法读取镜像root拥有的app/models/llm.py，exit5/0 tests。先更新计划并核对，再以与生产相同的UID0复验，保留cap-drop/只读/网络隔离，测试副本设为可读；不覆盖候选源码。两候选随后10/10通过。生产本来以root运行，本次并未宣称实现应用非root硬化。
- **Admin规范化路径**：公网smoke最初期待`/admin`直接302，实际308补斜线。记录后分别验证308目标确为`/admin/`、规范路径再302到登录；没有改变业务路由或弱化权限检查。

上述均发生在校验/预期边界，未修改被验收业务代码；不能把0 tests或不完整smoke报告成通过。

## 回滚与剩余工作

- 需要回滚时先核对当前任务状态，使用本次保留的旧应用镜像overlay；只更新四个应用服务，保留role/priority扩展列，不执行downgrade或恢复整库覆盖新数据。
- 不重跑旧首次安装、历史dump、通用deploy.sh/restore_db.sh，不down -v/prune/force push。
- 未进行真实付费模型、真实爬取或邮件验收；原有后台调度正常恢复。
- 并发硬预算、未知费用/崩溃窗口、lease/outbox、断路器Redis可靠性、Safe Fetch/SSRF与内容质量门禁仍在M1–M3。此次上线不代表动态Agent已实现。
- 下一阶段：M1.1新闻输出与声明式schema契约，继续TDD。
