# M2 closure release: recipe activation, claimed crawl runs, article LLM outbox (2026-09-25 09:41 UTC)

Owner-authorized ("选择1，合并入master之CI，全部完成之后进行部署"). Plan:
[2026-09-25-m2-release.md](../superpowers/specs/2026-09-25-m2-release.md). Design and implementation:
[spec](../superpowers/specs/2026-09-25-m2-activation-routing-design.md), [plan](../superpowers/plans/2026-09-25-m2-activation-routing.md),
[operations](../ops/crawl-runtime.md).

## Evidence before release

- Local full suite on the merged tree: 1229 passed, 26 skipped (baseline before the work: 1141 / 23).
- Whole-branch review by a fresh reviewer: 0 critical, 3 important (fixed with RED→GREEN tests: small dispatch
  batches, growing LLM re-send spacing, re-queue after a content change during processing), 11 minor (deferred,
  listed in the owner report).
- Master CI on `b32153d` (run 36115346994) failed `mysql-integration` on five test defects; corrected in
  `f0312b2` (details in the release plan). CI on `f0312b2` (run 36117503921): offline 1229 passed / 26 skipped,
  26 real-MySQL tests OK (incl. `compare_metadata` on the new tables and the concurrent-claim/job races), and the
  real Admin/Redis/prefork gate: `COUNT_RECYCLE_REAL_ADMIN_TASKS completed=100`, `MEMORY_RECYCLE_AFTER_COMPLETION
  rss_kib=452892`, `WORKER_LIFECYCLE_OK`.
- Read-only pre-check: 0 unprocessed articles (the backfill could not create paid work), 34 active sources.

## Run (owner via `!`, script `~/m2_release.sh f0312b2…`, tag `m2-20260925094001`)

| Time (UTC) | Step |
|---|---|
| 09:40:01 | Guards passed; checkout fast-forwarded `7ec578c` → `f0312b2` |
| 09:40:13 | Four images built from cache; Python and `pip freeze` identical to the running containers; new pin layer differs only in images |
| 09:40:15 | Ledger: 3333 settled reservations (330.600672 reserved / 0.929767 actual USD), 235 usage rows today (0.073845 USD) |
| 09:40:42 | Beat stopped; both workers replied with 0 active/reserved; queues llm/crawl/email/crawl_learn all 0; workers and web stopped |
| 09:40:48 | Backup `m2-20260925094001/database.sql.gz`, 20588032 bytes, 0600, SHA-256 `fc7c340d…828b7`, gzip and trailer verified |
| 09:40:52 | `flask db upgrade` `d3e7a1c95b28` → `b9d4f6a2c813`; new tables empty, 12 new columns present |
| 09:41:04 | Backfill dry run and `--apply`: 0 jobs |
| 09:41:48 | New web/worker/worker_fast up, health ok; llm worker registers `app.llm.article_tasks.process`, fast worker `app.crawlers.tasks.dispatch_due_crawls`; ledger unchanged; beat started last |
| 09:42:59 | First `dispatch_due_crawls` and `article_tasks.recover` succeeded; 34 state rows for 34 active sources; 4 claimed runs |
| 09:43:00 | `RELEASE_OK`; 0 restarts, no OOM, public health ok |

Service interruption (web and workers stopped) lasted about one minute.

## Post-release (read-only, 09:43:49)

- The first four claims were the four sources that had failed for weeks (oldest `last_crawled_at`), now
  classified instead of retried four times a day: STMicroelectronics and Inria Grenoble `failed/empty_extraction`
  (normal schedule, attention after three), CEA Enterprise `blocked/robots_unavailable` and CEA-List
  `blocked/tls_error` (24 h cooldown, attention flag).
- No live claims left over; one source due for the next round; no Traceback/CRITICAL in any container; the
  dispatcher and article recovery each succeeded; ledger today unchanged (235 settled).
- No article was created yet, so the first live article job is still to come.

## Rollback

Unchanged from the plan: stop beat, drain, then `up -d --no-build web worker worker_fast beat` with base + prod +
caddy + evidence + `~/fsourceinsight-backups/m34c-20260924171323/candidate.compose.json`. The additive schema stays.

## Not done

- No recipe is approved yet; every source still uses its legacy crawler (owner action in Admin).
- The first live `article_llm_job` and a successful claimed crawl with new articles were not yet observed.
- The four failing sources remain to be fixed separately (diagnosis 2026-09-24).
