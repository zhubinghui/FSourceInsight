# M1 直接受控发布（用户新授权）

## 授权与调整
- 用户明确要求“直接切换生产”，不再单独拆一个隔离部署测试阶段。此前仅准备的m1-deployment-validation计划由本发布计划取代；测试并入发布门禁，不承诺全部延伸验证已完成。
- 本轮允许提交、正常推送、SSH及生产增量迁移/四应用切换；仍单写者、不用子代理。不触发额外真实爬取/付费LLM/邮件，不自动发布任何recipe。M1新引擎仍是手工入口，日常schema路由属M2。
- 只更新web/worker/worker_fast/beat；不重启MySQL/Redis/Caddy，不干扰同机其他项目。不读取或打印.env、私钥、备份内容；不运行旧安装/恢复、seed、down -v、prune或force push。
- 本地基线master@f3644ca、459 passed/12专用skip；源代码全部保留。生产预期M0.5、d472，必须重新核对，不能借历史状态。

## 执行顺序
1. [complete] 本地/origin/生产均f3644ca，远端clean；生产health正常、四应用M0.5，其他容器未动。RAM可用4023MiB/磁盘22GiB；MySQL8.0.46/d472，无三质量列，Article约70.9MB/估计8546行、Dynamic/BTREE，无FULLTEXT。4个配置指纹ff4b48c9…（此次含role/priority），后续按同口径复核。
2. [in_progress] 复验本地，allowlist提交推送M1；CI通过（包括12项隔离MySQL）。
3. [pending] single-transaction备份并gzip/摘要检查，保留当前四镜像/ref/rollback overlay。构建真正候选，不覆盖候选app源码；至少候选依赖/模板/权限/新引擎及迁移门禁，不把历史镜像验证当本轮结果。
4. [pending] 备份/CI/候选通过后停beat、检查worker active/reserved/scheduled、warm-stop。有限metadata lock等待将d472升到e6（三个nullable列，不回填历史full/fr），验证model diff0与模型配置未变。
5. [pending] 只切四应用；健康/worker任务门禁失败自动回滚旧四镜像，保留扩展列，不downgrade/覆盖库。
6. [pending] 公网与匿名CSRF smoke、容器身份复核、精确清理本轮临时资源；归档发布证据，说明剩余M2–M5任务与未验证范围。

## 本轮验证偏差
- 本地pytest首次在约半数进度被120秒工具期限截断；检查无pytest/HTTP/parser残留进程，主机load约10/18/16，不能据此断定具体原因。改详细进度与600秒工具上限后同一代码459 passed/12 skipped/1919 warnings，76.48秒；无产品deadline/断言修改。证据/tmp/fsi-m1-release-local-tests.log。
- 55文件allowlist暂存后，cached diff检查发现此前未跟踪schema.py末尾多空行（旧diff检查不会检查未跟踪文件）；尚未commit/push，先仅删除尾部空行再重查，不改业务行为。
- 修改旧部署准备文档时误发非唯一`#`替换，工具拒绝、文件未变；改为读取准确标题后定点编辑。

## 风险与异常规则
- SafeFetch较旧直连严格，部分源可能因robots/域名范围停止采集；不为兼容直连绕过。原有自定义源不是全部迁移。
- 迁移扩展列和业务镜像回滚不同；DDL失败立即停下核对实际schema，不盲目重复非幂等ADD COLUMN。备份gzip检查不等于恢复演练。
- 任何权限、镜像、CI、schema或资源偏差先更新本计划再处理。生产进入停机窗口后必须明确恢复旧服务或完成切换，不留下无人管理的半停状态。
- 暂缓独立受控真实网络、长期prefork/FD/RSS压力和全来源金丝雀；不在发布成功后把这些说成已通过。

## 进度与证据
- 首次串行git fetch→SSH预检在60秒工具超时且无输出，无法按成功处理；尚未有远端动作证据。先拆分检查，git使用低速/连接边界，SSH显式ConnectTimeout及ServerAlive，避免把获取上游卡住误判成生产故障。拆分后fetch与SSH均成功，未提交/迁移/切换。
