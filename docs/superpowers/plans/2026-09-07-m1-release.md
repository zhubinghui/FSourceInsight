# M1 直接受控发布（用户新授权）

## 授权与调整
- 用户明确要求“直接切换生产”，不再单独拆一个隔离部署测试阶段。此前仅准备的m1-deployment-validation计划由本发布计划取代；测试并入发布门禁，不承诺全部延伸验证已完成。
- 本轮允许提交、正常推送、SSH及生产增量迁移/四应用切换；仍单写者、不用子代理。不触发额外真实爬取/付费LLM/邮件，不自动发布任何recipe。M1新引擎仍是手工入口，日常schema路由属M2。
- 只更新web/worker/worker_fast/beat；不重启MySQL/Redis/Caddy，不干扰同机其他项目。不读取或打印.env、私钥、备份内容；不运行旧安装/恢复、seed、down -v、prune或force push。
- 本地基线master@f3644ca、459 passed/12专用skip；源代码全部保留。生产预期M0.5、d472，必须重新核对，不能借历史状态。

## 执行顺序
1. [complete] 本地/origin/生产均f3644ca，远端clean；生产health正常、四应用M0.5，其他容器未动。RAM可用4023MiB/磁盘22GiB；MySQL8.0.46/d472，无三质量列，Article约70.9MB/估计8546行、Dynamic/BTREE，无FULLTEXT。4个配置指纹ff4b48c9…（此次含role/priority），后续按同口径复核。
2. [complete] 本地459/12，55文件297be85；测试快照修正1052edf仅测试/文档，业务输入diff为空。新CI34091467749两job通过（含12项MySQL）。
3. [complete] single-transaction备份m1-20260907062543及旧四镜像/ref/rollback overlay保留。1052edf再次构建真正候选，web/worker各12项MySQL通过（20.674/20.175s）；四镜像40模板/CSRF/Admin/HTTP标记/回放与billiard prefork→parser smoke通过。未覆盖app源码，实际UID0，cap-drop/只读rootfs/内部测试网络保持。
4. [complete] 停beat、两worker active/reserved/scheduled全0，warm-stop均exit0；有限metadata lock等待迁移e6成功，model diff0/配置指纹不变/历史标记NULL。
5. [complete] 2026-09-07 06:47:58Z→06:48:03Z四应用切换，health及worker注册/实际镜像门禁通过；未触发回滚。
6. [complete] 公网/匿名CSRF/Admin/API/HTML smoke及上线后DB/worker复核通过；只有四应用ID变化，其他容器ID/启动/重启数相同，无源码挂载。临时资源/凭据精确清理，helper归档。结果见[发布记录](../../audits/2026-09-07-m1-release.md)，剩余M2–M5另行实施。

## 本轮验证偏差
- 上线后HTTP脚本误向只读manage路径发送POST，得到405；此前首页/health/login/www/有效CSRF匿名settings拒绝已通过。先核对路由，用正确GET验证manage，不修改业务或把405误判为权限失败；完整公网smoke重跑通过。
- CI34090686950离线job通过，但新MySQL JSON/去重用例NoResultFound，12项中1 error。真正候选web baseline同样12项/1 error（21.077s）。专属副本边界probe实证`success no_change REPEATABLE READ fresh_rows 1 caller_rows 0`，排除未提交/解析失败：expire_all仅清ORM缓存，不结束fixture已有MySQL读快照。先修两项新测试的观察事务为session.remove，包含回滚负例，保持所有业务断言；不改引擎、不让引擎擅自提交调用者事务。probe仅测试副本、未改候选app。修复fixture后真正web/worker各12项全通过（21.338/20.725s），临时probe已被正式测试覆盖。只提交测试/文档改动，再CI及更新checkout/候选输入，不改业务实现。
- Docker29 image inspect模板`.Config.User`字段缺失时报错；不会据此猜测试UID。改用受限`id`运行检查实际身份，不打印整个Config/Env。
- 本地pytest首次在约半数进度被120秒工具期限截断；检查无pytest/HTTP/parser残留进程，主机load约10/18/16，不能据此断定具体原因。改详细进度与600秒工具上限后同一代码459 passed/12 skipped/1919 warnings，76.48秒；无产品deadline/断言修改。证据/tmp/fsi-m1-release-local-tests.log。
- 55文件allowlist暂存后，cached diff检查发现此前未跟踪schema.py末尾多空行（旧diff检查不会检查未跟踪文件）；尚未commit/push，先仅删除尾部空行再重查，不改业务行为。
- 修改旧部署准备文档时误发非唯一`#`替换，工具拒绝、文件未变；改为读取准确标题后定点编辑。

## 风险与异常规则
- SafeFetch较旧直连严格，部分源可能因robots/域名范围停止采集；不为兼容直连绕过。原有自定义源不是全部迁移。
- 迁移扩展列和业务镜像回滚不同；DDL失败立即停下核对实际schema，不盲目重复非幂等ADD COLUMN。备份gzip检查不等于恢复演练。
- 任何权限、镜像、CI、schema或资源偏差先更新本计划再处理。生产进入停机窗口后必须明确恢复旧服务或完成切换，不留下无人管理的半停状态。
- 独立真实新闻源、长期prefork/FD/RSS压力和全来源金丝雀暂缓；仅允许无业务凭据候选以SafeFetcher访问本站https://fsourceinsight.eu/health及其robots.txt（显式唯一主机、15秒/4请求/有界字节）验证真实DNS/socket/TLS/CA，不抓新闻、不访问其他域名。基础billiard smoke不是完整Celery/broker压力测试。

## 最终结果
- M1应用1052edf/schema e6已部署，06:48:03Z恢复健康，后续DB/worker/公网复核成功，未回滚。完整[证据与回滚点](../../audits/2026-09-07-m1-release.md)已记录，下一步M2。
- 一次辅助CI日志查询误用job ID退出1，没有改变CI状态；随后从正确run34091467749日志取得459/12（98.60s）与MySQL12项（12.831s）通过证据。

## 历史进度与证据（按当时状态保留）
- 四候选smoke通过；SafeFetcher只访问本站health/robots的真实DNS/socket/TLS/CA探针通过。临时MySQL/runner/网络/卷/凭据已按精确label及名称删除，生产仍旧容器。准备执行已落盘activate脚本，ERR/信号按阶段恢复旧服务，只有健康/worker/镜像一致才标complete。
- 55文件已提交297be8568f24063b73b8b08862a7dab696e5d7d1并正常推送，提交前只删除schema.py多余EOF空行。CI34090686950一项失败；备份m1-20260907062543（18,637,439 bytes/mode600/SHA256 2a1f1883372821234ba2bdfae48c41fdb1c8e39248160c0d181cd2a5cb400af0）及真正四候选已构建，旧应用运行未变，后续先修门禁失败。
- 首次串行git fetch→SSH预检在60秒工具超时且无输出，无法按成功处理；尚未有远端动作证据。先拆分检查，git使用低速/连接边界，SSH显式ConnectTimeout及ServerAlive，避免把获取上游卡住误判成生产故障。拆分后fetch与SSH均成功，未提交/迁移/切换。
