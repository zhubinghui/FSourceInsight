# M0.5 提交与受控部署

## 授权 / 范围
- 用户明确要求“先完成一下提交部署”；本轮提交、推送、部署已验收的M0.5，暂不进入M1。
- 主会话单写者；生产代码预期d91863f、运行858a14b镜像、schema c821。任何差异先记录再处理，不覆盖远端改动。
- 仅更新web/worker/worker_fast/beat及增量迁移d472。MySQL/Redis/Caddy和同机其他应用不重启/重配；不读.env密钥/私钥/备份内容。
- 不运行旧首次安装、通用deploy.sh/restore_db.sh、历史dump恢复、down -v、prune或force push。
- 不手工触发真实爬取、付费LLM、邮件；后台任务只按正常原有调度恢复。

## 阶段
1. [complete] 本地与origin/服务器均d91863f，远端干净；本地131 passed/10远程专用skip、diff/静态错误检查通过。服务器3976MiB可用RAM/22GiB磁盘；当前c821无role/priority，4个配置指纹e33b89e899f99e1bea1cc8255cfe566dfd9b6a9f0d404fa1ae8eb78a219733a5；运行镜像与隔离验收时一致。
2. [complete] 30文件allowlist提交6451b36并正常推送；GitHub run34036731442的test/mysql-integration均success。
3. [complete] /home/ubuntu/fsourceinsight-backups/m05-20260906134051保存旧ref/镜像/容器/rollback overlay，数据库single-transaction备份18,613,221 bytes/mode600/gzip通过，SHA256 b717eaf749f4e1485c496974db34b1608a2bb647b0c589309e3228c09597f7fa。
4. [complete] 实际候选web093e447a…与worker8369b86e…各10/10 MySQL通过（15.977/15.880秒）；四个镜像均40模板、匿名有效CSRF、Admin路由与无env/Git/dump smoke通过。只挂载测试副本，不覆盖候选业务源码；权限偏差已按下述记录解决。
5. [complete] CI/候选/备份通过；候选临时资源/凭据已精确清理，原容器未变。暂停beat后再次确认两worker全部空闲，以-t -1 warm-stop正常exit0。
6. [complete] 文件脚本明确stdin，迁移d472成功（DDL lock_wait_timeout=10）；model diff0、4个配置指纹未变。13:53:46Z→13:53:51Z四应用切换成功，Web约5秒恢复，LIVE_WORKERS_READY。未触发应用回滚。
7. [complete] 公网首页/health/login/www均200；匿名有效CSRF、manage和规范化Admin路径正确302拒绝。生产d472/model diff0、配置指纹不变、worker就绪、MySQL/Redis/其他容器身份不变。临时资源/凭据已清理，辅助脚本/证据归档；发布记录随最终文档提交同步，不重建镜像。

## 偏差与处理
- 首轮实际候选MySQL runner在app导入时exit5、0 tests：`PermissionError: /app/app/models/llm.py`。发布准备脚本umask077使git更新源码为600，Docker COPY保留权限；生产默认UID0可读，而额外加的UID1000测试身份不可读。当前只做候选验证，未迁移/切换。复验将使用与生产一致的UID0，继续cap-drop ALL/只读rootfs/受限网络；测试文件复制到单独只读目录并以644/755供runner读取，不覆盖候选app源码，也不放宽生产.env或备份权限。保留首轮错误日志，不把0 tests视为通过。

- 上线后独立HTTP smoke首次把`/admin`直接预期302而得到308：Flask补尾斜线，非权限失败。先记录，再对`/admin`→`/admin/`的308位置和规范路径的302登录拒绝分别验收，不修改业务路由或弱化权限断言。其余已确认health/DB/worker及容器身份检查通过。

## 发布结果
- 应用代码6451b3641cc6c8934d381aefd65f7efdb8d40d6f已部署；本计划完成。最终证据和回滚点见 [M0.5发布记录](../../audits/2026-09-06-m05-release.md)。

## 回滚与限制
- 新迁移仅增加llm_config.role/priority，旧配置任务/模型不覆盖；不重跑seed。应用回滚保留新增列，不执行downgrade。
- 旧镜像与旧代码ref保留；回滚不恢复全量备份覆盖新增数据。备份未做全量恢复演练时明确记录。
- v2缓存首次发布冷启动，严格JSON可能增加失败率；不批量force重算。M2/M3硬预算、lease/outbox、断路器Redis可靠性和M1 Safe Fetch仍未实现。
- 既有隔离验证是源码覆盖旧依赖镜像，两轮10/10；本轮真正候选必须重新验收，不拿旧报告替代。
